# JWT设置
SECRET_KEY=dev-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

 # 超级管理员设置
SUPER_ADMIN_EMAIL=admin@chatx.com
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_PASSWORD=ChatX@Admin123!
SUPER_ADMIN_FULL_NAME="Super Administrator"

# PostgreSQL数据库设置
POSTGRES_USER=chatx_user
POSTGRES_PASSWORD=chatx_password
POSTGRES_DB=chatx_db
POSTGRES_PORT=5433
POSTGRES_CONTAINER_NAME=chatx_postgres
DATABASE_URL=postgresql://chatx_user:chatx_password@localhost:5433/chatx_db

# Redis设置
REDIS_URL=redis://localhost:6380/0
REDIS_PORT=6380
REDIS_CONTAINER_NAME=chatx_redis

# MinIO设置
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=chatx_minio
MINIO_SECRET_KEY=chatx_minio_password
MINIO_SECURE=false
MINIO_BUCKET=chatx-files
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_CONTAINER_NAME=chatx_minio

# Weaviate设置
WEAVIATE_URL=http://localhost:8080
WEAVIATE_PORT=8080
WEAVIATE_CONTAINER_NAME=chatx_weaviate

# Neo4j知识图谱设置
NEO4J_USER=neo4j
NEO4J_PASSWORD=chatx_neo4j_password
NEO4J_URL=bolt://localhost:7687
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687
NEO4J_CONTAINER_NAME=chatx_neo4j
NEO4J_HEAP_INITIAL=512m
NEO4J_HEAP_MAX=1G
NEO4J_PAGECACHE=256m

# CORS设置
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# 文件上传设置
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf,text/plain,application/json

# 应用设置
APP_PORT=8000
APP_CONTAINER_NAME=chatx_app

# Celery设置
CELERY_LOG_LEVEL=info
CELERY_WORKER_CONTAINER_NAME=chatx_celery_worker
CELERY_BEAT_CONTAINER_NAME=chatx_celery_beat

# Nginx设置
NGINX_CONTAINER_NAME=chatx_nginx
HTTP_PORT=80
HTTPS_PORT=443
DOMAIN_NAME=localhost

# Docker网络设置
NETWORK_NAME=chatx_network

# 开发环境设置
DEBUG=true
ENVIRONMENT=development